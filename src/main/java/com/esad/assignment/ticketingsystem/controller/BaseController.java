package com.esad.assignment.ticketingsystem.controller;

import java.util.logging.Logger;

public class BaseController {
    protected final Logger logger = Logger.getLogger(this.getClass().getName());
}
